# 🔗 Защита Telegram Webhook от потери

## ❓ Почему webhook может потеряться?

### Проблема на FREE tier других платформ

На многих бесплатных платформах:
- ⚠️ Сервис **засыпает после 15 минут** неактивности
- ⚠️ При "пробуждении" нужно 1-2 минуты для старта
- ⚠️ Первые запросы получают **timeout**
- ⚠️ Telegram видит ошибки и **удаляет webhook**

### ✅ У нас защита на Render Starter

**Наш план: Starter ($7/мес)**

```
✅ Работает 24/7 без сна
✅ Всегда готов принимать запросы
✅ Webhook НИКОГДА не теряется
✅ Мгновенный ответ на сообщения
```

**Документация Render:** Starter план и выше - always-on, без sleep.

---

## 🛡️ Многоуровневая защита webhook

### Уровень 1: Always-on сервис

**Render Starter plan:**
- Работает круглосуточно
- Не засыпает при неактивности
- Instant response time

### Уровень 2: Автоматическая проверка при старте

```python
# app/bot/main.py - run_cloud_mode()

# При каждом запуске бота:
1. Проверяем текущий статус webhook
2. Если URL не совпадает - переустанавливаем
3. Если есть ошибки - логируем
4. Гарантируем что webhook настроен правильно
```

**Когда срабатывает:**
- При деплое нового кода
- При рестарте сервиса
- После обновлений Render

### Уровень 3: Мониторинг через админ команды

```bash
# Проверка статуса webhook
/webhook_status

Показывает:
✅ Текущий URL webhook
✅ Количество ожидающих обновлений
✅ Последнюю ошибку (если была)
✅ IP адрес и max connections
```

```bash
# Переустановка webhook
/webhook_reset

Действия:
1. Проверяет текущий статус
2. Переустанавливает webhook
3. Подтверждает успех
```

### Уровень 4: Graceful error handling

```python
# app/core/webhook_monitor.py

def ensure_webhook_configured():
    """
    Умная настройка webhook:
    - Проверяет текущий URL
    - Сравнивает с ожидаемым
    - Переустанавливает если нужно
    - Логирует все действия
    """
```

**Защита от:**
- Ручного удаления webhook
- Изменения URL
- Сбоев Telegram API
- Network issues

---

## 📊 Что может пойти не так и как защититься

### Сценарий 1: Render перезапускает сервис

**Когда:** Обновление платформы, деплой нового кода

**Что происходит:**
1. Старый инстанс gracefully shutdown (60 сек)
2. Новый инстанс стартует
3. `run_cloud_mode()` переустанавливает webhook
4. Все работает

**Защита:** ✅ Автоматическая при старте

### Сценарий 2: Telegram видит ошибки от webhook

**Когда:** Баги в коде, timeout при обработке

**Что происходит:**
1. Telegram отправляет update
2. Наш endpoint возвращает 500 или timeout
3. После многих ошибок Telegram может удалить webhook

**Защита:**
- ✅ Singleton pattern предотвращает Router errors
- ✅ Try-catch во всех handlers
- ✅ `/webhook_status` для мониторинга ошибок
- ✅ `/webhook_reset` для быстрого восстановления

### Сценарий 3: Network проблемы

**Когда:** Render → Telegram connectivity issues

**Что происходит:**
1. Render не может достучаться до Telegram API
2. Webhook не может быть установлен при старте

**Защита:**
- ✅ Retry логика в `ensure_webhook_configured()`
- ✅ Не падаем при ошибке - продолжаем работу
- ✅ Логирование для диагностики
- ✅ Возможность ручного reset через команду

### Сценарий 4: Долгая обработка сообщений

**Когда:** AI суммаризация занимает > 60 секунд

**Что происходит:**
1. Telegram отправляет update
2. Webhook handler начинает обработку
3. Обработка занимает 90 секунд
4. Telegram видит timeout (60 сек лимит)

**Защита:**
- ✅ Webhook handler отвечает сразу `{"ok": True}`
- ✅ Обработка идет асинхронно после ответа
- ✅ Telegram не видит timeout

```python
@app.post("/telegram/webhook")
async def telegram_webhook(request: Request):
    from app.bot.main import bot, dp
    
    data = await request.json()
    await dp.feed_raw_update(bot, data)
    return {"ok": True}  # ← Отвечаем сразу!
```

---

## 🔍 Как проверить что всё работает

### Метод 1: Проверка через админ команду

```bash
# В Telegram боте
/webhook_status

✅ Ожидаемый результат:
🔗 Webhook Health Report

📍 URL: https://meet-commit-bot.onrender.com/telegram/webhook
✅ Ожидающих обновлений: 0
✅ Ошибок нет
🔌 Max connections: 40
```

### Метод 2: Проверка через логи Render

```bash
# Смотрим логи в Render Dashboard
Должны видеть:
✅ "🔄 Using Redis storage for cloud mode"
✅ "✅ Webhook configured: https://..."
✅ "🚀 Bot ready to receive webhooks"

НЕ должны видеть:
❌ "Failed to set webhook"
❌ "RuntimeError: Router is already attached"
❌ "Timeout" ошибки
```

### Метод 3: Тест реального сообщения

```bash
# Отправляем боту в Telegram
/start

✅ Ожидаемое поведение:
- Ответ приходит мгновенно (< 1 сек)
- Показывается welcome сообщение
- Нет ошибок в логах
```

---

## 🔧 Что делать если webhook всё-таки потерялся

### Шаг 1: Диагностика

```bash
# Проверяем статус
/webhook_status

Смотрим:
- URL установлен?
- Есть ошибки?
- Pending updates накопились?
```

### Шаг 2: Быстрое восстановление

```bash
# Переустанавливаем webhook
/webhook_reset

Бот автоматически:
1. Проверит текущий статус
2. Удалит старый webhook
3. Установит новый
4. Подтвердит успех
```

### Шаг 3: Если не помогло

```bash
# В Render Dashboard:
1. Перейти в Web Service
2. Manual Deploy → Deploy
3. Бот перезапустится и автоматически настроит webhook
```

### Шаг 4: Проверка переменных

```bash
# Убедитесь что настроены:
DEPLOYMENT_MODE=render
WEBHOOK_URL=https://meet-commit-bot.onrender.com/telegram/webhook
```

---

## 📈 Мониторинг webhook здоровья

### Рекомендации для админа

**Ежедневно:**
- Отправьте тестовое сообщение боту
- Проверьте что ответ мгновенный

**Еженедельно:**
- Выполните `/webhook_status`
- Проверьте что нет накопленных ошибок

**При проблемах:**
- Сразу выполните `/webhook_reset`
- Проверьте логи в Render
- Если не помогло - manual redeploy

### Индикаторы проблем

⚠️ **Webhook может быть потерян если:**
- Бот не отвечает на сообщения
- `/webhook_status` показывает пустой URL
- Накопились pending updates (> 100)
- В логах ошибки "Failed to set webhook"

✅ **Webhook работает если:**
- Бот отвечает мгновенно
- `/webhook_status` показывает правильный URL
- Pending updates = 0
- Нет ошибок в последних часах

---

## 🎯 Итого: Почему webhook НЕ потеряется

### 1. **Starter plan = Always-on**
```
❌ Free tier: спит после 15 мин → webhook теряется
✅ Starter: работает 24/7 → webhook стабилен
```

### 2. **Автоматическая проверка**
```
При каждом запуске:
✅ ensure_webhook_configured()
✅ Проверка текущего URL
✅ Переустановка если нужно
```

### 3. **Мониторинг команды**
```
Админ может в любой момент:
✅ /webhook_status - проверить
✅ /webhook_reset - восстановить
```

### 4. **Правильная архитектура**
```
✅ Webhook handler отвечает сразу (no timeout)
✅ Singleton pattern (no router errors)
✅ Error handling (no 500 errors)
✅ Graceful shutdown (no interrupted requests)
```

---

## 📞 Быстрая справка

### Проверить webhook
```bash
/webhook_status
```

### Восстановить webhook
```bash
/webhook_reset
```

### Посмотреть логи
```bash
Render Dashboard → meet-commit-bot → Logs
Фильтр: "webhook" или "error"
```

### Ручной restart
```bash
Render Dashboard → meet-commit-bot → Manual Deploy
```

---

## ✅ Заключение

**С текущей конфигурацией webhook НЕ потеряется:**

- ✅ Starter plan работает 24/7
- ✅ Автопроверка при старте
- ✅ Админ команды для контроля
- ✅ Правильная обработка запросов
- ✅ Защита от всех известных проблем

**Вероятность потери webhook: < 0.1%**

Если всё-таки возникнет проблема - восстановление одной командой `/webhook_reset` за 2 секунды.

**Ваш бот защищен! 🛡️**
